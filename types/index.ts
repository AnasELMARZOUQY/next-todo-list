export interface Country {
    name: string;
  }
  
  export interface User {
    name: string;
    country: string;
    description: string;
  }
  
  export interface Todo {
    user: string;
    country: string;
    description: string;
  }